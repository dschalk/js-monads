// import {ret, Monad, MonadIter, MonadStream, add, cube, push, equals, shift, splice, map, filter, reduce, unshift, calc} from './monad.js'
import mM from './monad.js';
var m = new mM.Monad(12, 'm');
console.log(m);
